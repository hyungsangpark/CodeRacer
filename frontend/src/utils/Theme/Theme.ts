import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    mode: "dark",
    background: {
      default: "#121212",
    },
    primary: {
      light: "#adff00",
      main: "#4fcc4d",
      dark: "#3DA93B",
    },
  },
});

export default theme;
